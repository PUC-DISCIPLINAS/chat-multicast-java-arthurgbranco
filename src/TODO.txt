addServerCommands:
    - !create x: "creates room x"
    - !delete x: "deletes room x"
    - !exit: "Closes the server"

Server:
    - Server should create room 0 as default
    - Server should print info on data beeing sent (low priority)

Client:
    - Client should check if room exists before joining (on Server rooms array)
    - Client should join room 0 as default and display "Server no initialized if there is no room 0"

object:
    - Class that serializes objects before sending the packets and deserializes them before others classes can use it